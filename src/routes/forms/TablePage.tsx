import React from "react";
import { FormattedMessage } from "react-intl";

const TablePage: React.FC = () => {
  return (
    <div>
      <div>
        <FormattedMessage id="table.title" defaultMessage="Table page" />
      </div>
    </div>
  );
};

export default TablePage;
