import React from "react";
import { FormattedMessage } from "react-intl";

const InputPage: React.FC = () => {
  return (
    <div>
      <FormattedMessage id="input.title" defaultMessage="Input page" />
    </div>
  );
};

export default InputPage;
